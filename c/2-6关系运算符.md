```
1. > >= < <= == !==
2. > >= < <= 优先级高于 == !=
3. 关系运算符优先级低于算数运算符 i < lim - 1 等于 i < (lim-1)
4. 逻辑运算符 && ||，从左到右求值，在知道结果为真或假时立即停止。
5. &&优先级高于||，两者都比关系运算符高。
6. !=优先级高于=。
7. 逻辑非运算符!，将非0转成0，将0转换成1。
```