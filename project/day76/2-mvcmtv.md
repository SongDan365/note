### mvc mtv
```py
MVC,MTV
    
    models(数据库，模型)   views（html模板）    controllers（业务逻辑处理）    --> MVC


    models(数据库，模型)   templates(html模板)  views（业务逻辑处理）          --> MTV
    
    
    Django -> MTV
```
